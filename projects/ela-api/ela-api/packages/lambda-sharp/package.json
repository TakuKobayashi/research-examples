{
  "name": "@ela-api/lambda-sharp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && cp package.json dist/",
    "start": "node dist/index.js",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "package": "npm run build && cd dist && zip -r ../lambda-sharp.zip . && cd ..",
    "deploy": "aws lambda update-function-code --function-name ela-api-sharp --zip-file fileb://lambda-sharp.zip"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.3",
    "hono": "^4.7.10",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.15.21",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}
