{
  "name": "cloudflare-hono-sample",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "d1:database:create": "wrangler d1 create",
    "d1:migration:create": "wrangler d1 migrations create",
    "d1:migration:apply": "wrangler d1 migrations apply"
  },
  "dependencies": {
    "hono": "^4.4.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "wrangler": "^3.57.2"
  }
}